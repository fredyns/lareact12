APP_NAME=LaReact12
APP_ENV=local
APP_KEY=base64:FfmVYI5SM3doFDJHDKY0ouVI8QYHw3N28PiN50+/+zM=
APP_DEBUG=true
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Docker Database Configuration
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=lareact12
DB_USERNAME=postgres
DB_PASSWORD=postgres

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Docker Broadcast Configuration
BROADCAST_DRIVER=soketi
BROADCAST_CONNECTION=soketi
PUSHER_APP_ID=1
PUSHER_APP_KEY=app-key
PUSHER_APP_SECRET=app-secret
PUSHER_HOST=soketi
PUSHER_PORT=6001
PUSHER_SCHEME=http

FILESYSTEM_DISK=minio
QUEUE_CONNECTION=redis

# Soketi Configuration
SOKETI_HOST=localhost
SOKETI_PORT=6001
SOKETI_SCHEME=http
SOKETI_APP_ID=1
SOKETI_APP_KEY=app-key
SOKETI_APP_SECRET=app-secret

# Docker Cache Configuration
CACHE_STORE=redis
CACHE_PREFIX="${APP_NAME}_"

MEMCACHED_HOST=127.0.0.1

# Docker Redis Configuration
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
VITE_TINYMCE_API_KEY=kqlcok1ov2gwyg8pxf74wan2cgrvq1uj0ztg3al7n5y0emt2

# Docker MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET_NAME=localhost
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123

# Sentry Configuration
SENTRY_LARAVEL_DSN=http://6eef1855c9b07fa32a61262bc2b4cecc@localhost:9002/2
